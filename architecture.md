### 架构文档 1：nRF52840 固件端 (基于 ZMK)

**核心定位：** 绝对的“提线木偶”。不保存任何游戏逻辑，不记忆任何动作组合。只负责三件事：解析串口报文、调用 ZMK 事件注入、维持防抱死心跳。

**1. 通信协议设计 (定长/极简字符串)**

为了极致的解析速度和稳定性，废除长字符串和正则，改用 `模块:指令:参数` 的 3 段式短文本协议，以 `\n` 结尾。

| **模块**    | **指令**        | **参数说明**           | **示例**      | **含义**            |
| ----------- | --------------- | ---------------------- | ------------- | ------------------- |
| **K**(键盘) | **D**(Down)     | HID Usage ID (Hex格式) | `K:D:04\n`    | 按下 A 键 (0x04)    |
| **K**(键盘) | **U**(Up)       | HID Usage ID (Hex格式) | `K:U:04\n`    | 松开 A 键           |
| **K**(键盘) | **A**(All)      | 无参                   | `K:A\n`       | 释放所有按键        |
| **M**(鼠标) | **R**(Relative) | X, Y 坐标位移 (10进制) | `M:R:15:-5\n` | 鼠标向右 15，向上 5 |
| **M**(鼠标) | **C**(Click)    | L(左)/R(右)/M(中)      | `M:C:L\n`     | 单击鼠标左键        |
| **M**(鼠标) | **D**(Down)     | L(左)/R(右)/M(中)      | `M:D:L\n`     | 按下鼠标左键        |
| **M**(鼠标) | **U**(Up)       | L(左)/R(右)/M(中)      | `M:U:L\n`     | 松开鼠标左键        |
| **S**(系统) | **P**(Ping)     | 无参                   | `S:P\n`       | 心跳保活指令        |

**2. 核心模块重构建议**

- **指令解析器 (Parser)：** 替换你现有的 `process_line`。利用 C 语言的 `strtok` 或者简单的数组索引切割字符串，直接将十六进制按键码转换为 ZMK 需要的内部事件。这比目前的 `keycode_from_name` 查表法效率高得多，且支持所有 104 键。
- **看门狗/防抱死机制 (Failsafe)：** 这是重中之重。在 ZMK 中使用 Zephyr 的 `k_work_delayable` 创建一个延时任务，时长设为 500ms。
- **心跳重置：** 每次串口接收到任意有效指令（包括 `S:P`），立刻取消并重新提交这个延时任务。
- **抱死触发：** 如果 500ms 内没有收到上位机消息，延时任务触发，执行 `Release All`，清空 ZMK 的按键状态矩阵，防止游戏角色失控。
- **鼠标支持：** ZMK 默认禁用鼠标功能。你需要在 `.conf` 文件中开启 `CONFIG_ZMK_MOUSE=y`，并通过 ZMK 内部的 `zmk_mouse` API 注入相对坐标事件。
